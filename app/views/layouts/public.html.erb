<!DOCTYPE html>
<html>
  <head>
    <title><%= @wedding&.title || "Wedding" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body class="font-serif m-0 bg-stone-50 text-neutral-800 leading-relaxed">
    <main class="min-h-[calc(100vh-100px)]">
      <%= yield %>
    </main>

    <footer class="bg-stone-700 text-white text-center p-8 mt-16">
      <p class="m-0 text-stone-300">&copy; <%= Time.current.year %> <%= @wedding&.title || "Wedding" %></p>
    </footer>
  </body>
</html>
