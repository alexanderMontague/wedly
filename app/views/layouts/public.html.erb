<!DOCTYPE html>
<html>
  <head>
    <title><%= @wedding&.title || "Wedding" %> - Wedly</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
    <style>
      :root {
        --primary-color: <%= @wedding&.primary_color || '#C89B7B' %>;
      }
    </style>
  </head>

  <body class="font-serif m-0 bg-[#faf9f7] text-gray-800 leading-relaxed">
    <main class="min-h-[calc(100vh-100px)]">
      <%= yield %>
    </main>

    <footer class="bg-secondary text-white text-center p-8 mt-16">
      <p>&copy; <%= Time.current.year %> <%= @wedding&.title || "Wedding" %>. Powered by Wedly.</p>
    </footer>
  </body>
</html>
