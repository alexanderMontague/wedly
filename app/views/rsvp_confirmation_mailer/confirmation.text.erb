THANK YOU

Your RSVP is confirmed

We've received your response — here's what we have on file for your party:
<% @guests.each do |guest| -%>
<%
  label = case guest.rsvp_status
          when "accepted" then "Joyfully accepts"
          when "declined" then "Regretfully declines"
          else "Awaiting reply"
          end
-%>
  - <%= guest.full_name %>: <%= label %><%= guest.rsvp&.meal_choice.present? ? " (#{guest.rsvp.meal_choice})" : "" %>
<% end -%>
--------------------------------------------------

Need to make a change? You may update your response any time before the big
day:

<%= @rsvp_url %>

We can't wait to celebrate with you.
<%= @wedding.title %>
